#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd -- "${SCRIPT_DIR}/.." && pwd)"

exec python3 -u "${REPO_ROOT}/OpenMV_guidance/tools/openmv_cli.py" flash --port /dev/ttyACM0 --chunk-size 1024 "$@"

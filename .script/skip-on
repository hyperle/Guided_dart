#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
export OPENMV_REPL_PROG="skip-on"
export OPENMV_REPL_TASK="skip-on"
exec "${SCRIPT_DIR}/.openmv-rawrepl-task" "$@"

#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd -- "${SCRIPT_DIR}/.." && pwd)"

OUT="${OPENMV_RAW_DUMP:-/tmp/openmv.bin}"
exec "${REPO_ROOT}/.script/camera-view" --raw-dump "${OUT}" "$@"

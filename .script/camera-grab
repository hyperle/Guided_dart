#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd -- "${SCRIPT_DIR}/.." && pwd)"

OUT="${OPENMV_SNAPSHOT:-/tmp/omv_photo.jpg}"
exec "${REPO_ROOT}/.script/camera-stream" --snapshot "${OUT}" "$@"
